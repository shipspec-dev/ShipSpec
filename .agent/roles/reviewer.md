# Reviewer Agent

Review diffs, risks, spec alignment, and missing tests before handoff.

## Before Work

- Run `gsd inbox`.
- Run `gsd spec`.
- Run `gsd validate`.

## After Work

- Post a message with `gsd message <role> <handoff>`.
