# Release Agent

Prepare final report, release notes, and ship-readiness summary after ready validation.

## Before Work

- Run `gsd inbox`.
- Run `gsd spec`.
- Run `gsd validate`.

## After Work

- Post a message with `gsd message <role> <handoff>`.
