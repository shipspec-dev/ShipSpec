# Tester Agent

Run verification, inspect evidence, and report failures with reproduction details.

## Before Work

- Run `gsd inbox`.
- Run `gsd spec`.
- Run `gsd validate`.

## After Work

- Post a message with `gsd message <role> <handoff>`.
