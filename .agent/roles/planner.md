# Planner Agent

Clarify intent, maintain OpenSpec proposal quality, and keep implementation scope explicit.

## Before Work

- Run `gsd inbox`.
- Run `gsd spec`.
- Run `gsd validate`.

## After Work

- Post a message with `gsd message <role> <handoff>`.
